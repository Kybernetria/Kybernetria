---
import { getCollection } from 'astro:content';
import SectionDivider from './SectionDivider.astro';

interface Props {
  limit?: number;
  showHeading?: boolean;
}

const { limit, showHeading = true } = Astro.props;
const base = '/';

let posts = await getCollection('blog', ({ data }) => {
  return import.meta.env.PROD ? data.draft !== true : true;
});

posts = posts.sort((a, b) => b.data.date.valueOf() - a.data.date.valueOf());
if (limit) {
  posts = posts.slice(0, limit);
}
---

<SectionDivider code="S3" title="WRITING" />

<section id="transmissions-section" class="w-full bg-forge py-24 px-6 md:px-12">
  <div class="max-w-7xl mx-auto">
    
    {showHeading && (
      <div class="flex items-center justify-between mb-12 border-b border-wire pb-4">
        <h2 class="font-subhead text-2xl uppercase tracking-widest text-chalk">Recent Writing</h2>
        <a href={`${base}blog/`} class="font-mono text-sm text-signal hover:text-copper transition-colors">View All →</a>
      </div>
    )}

    <div class="grid grid-cols-1 md:grid-cols-2 xl:grid-cols-3 gap-8">
      {posts.map((post) => (
        <a href={`${base}blog/${post.id.replace(/\.md$/, '')}/`} class="block group relative bg-plate border border-transparent rounded-xl hover:border-wire transition-colors duration-300 overflow-hidden">
          {post.data.heroImage && (
            <div class="w-full aspect-video overflow-hidden">
              <img src={post.data.heroImage} alt="" class="w-full h-full object-cover grayscale opacity-80 group-hover:grayscale-0 group-hover:opacity-100 transition-all duration-500" />
            </div>
          )}
          <div class="p-6 md:p-8">
            <time datetime={post.data.date.toISOString()} class="block font-mono text-xs text-ash mb-4">
              {post.data.date.toISOString().split('T')[0]}
            </time>
            <h3 class="font-subhead text-[1.3rem] font-semibold text-chalk group-hover:text-signal transition-colors duration-200 mb-3 leading-tight">
              {post.data.title}
            </h3>
            <p class="font-body text-[0.875rem] text-ash line-clamp-3 mb-6">
              {post.data.excerpt}
            </p>
            <div class="flex flex-wrap gap-2 mb-6">
              {post.data.tags.map((tag) => (
                <span class="inline-block border border-wire rounded-full px-3 py-1 font-mono text-[0.7rem] uppercase text-ash hover:bg-copper hover:text-carbon hover:border-transparent transition-colors duration-200">
                  {tag}
                </span>
              ))}
            </div>
            <div class="flex items-center">
              <span class="font-subhead uppercase text-sm text-signal opacity-0 -translate-x-2 group-hover:opacity-100 group-hover:translate-x-0 transition-all duration-300">
                Read →
              </span>
            </div>
          </div>
        </a>
      ))}
    </div>
  </div>
</section>
